##############################
# mod_upload.R (optional stub)
##############################
# Reserved for future: user uploads of counts/positions/image. Not used now.

mod_upload_ui <- function(id) { NS(id); tagList() }
mod_upload_server <- function(id) { moduleServer(id, function(input, output, session) {}) }

